# fixme2.py

```bash
┌──(kalilinux)-[~/picoCTF]
└─$ python3 runme2.py
"That is correct! Here's your flag: picoCTF{🏁}"
                
┌──(kalilinux)-[~/Documents/picoCTF]
└─$ 
```

### 1. **Comando `python3 runme2.py`:**
   - Este comando ejecuta el archivo Python llamado `runme2.py` utilizando Python 3.
   - El script parece validar alguna condición o desafío, y si se cumple la condición, te muestra un mensaje que incluye la bandera.

### 2. **Mensaje mostrado:**
   ```bash
   "That is correct! Here's your flag: picoCTF{🏁}"
   ```
   - Este mensaje confirma que has ejecutado correctamente el script o completado el desafío.
   - Luego te proporciona la bandera, que sigue el formato estándar de picoCTF: `picoCTF{🏁}`.

### ¿Qué está sucediendo?
El script `runme2.py` probablemente contiene alguna lógica de validación o solución, donde al ejecutarlo correctamente, el programa te muestra la bandera. Puede ser que el script haya verificado un valor, resuelto un enigma o ejecutado un código cifrado para generar este mensaje.
