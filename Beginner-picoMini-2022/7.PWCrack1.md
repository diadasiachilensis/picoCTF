# PWCrack1

Aquí te doy una explicación detallada de lo que hace el código en tu terminal, con especial atención a la modificación que mencionas en el condicional `if( user_pw == "8713"):`:

### Resumen del código:
Este script de Python (`level1.py`) es parte de un desafío de CTF. Su propósito es verificar si el usuario ingresa la contraseña correcta (`8713`). Si la contraseña es correcta, descifra un archivo cifrado (`level1.flag.txt.enc`) usando una función de XOR y luego muestra la bandera (`picoCTF{🏁}`) al usuario.

### Modificación en el condicional:
```python
if( user_pw == "8713"):
```
Esta línea compara la entrada del usuario (`user_pw`) con el valor `"8713"`. Si el valor que el usuario ingresa coincide exactamente con `"8713"`, entonces el programa procederá a descifrar el archivo cifrado y mostrar la bandera. Si la contraseña no coincide, mostrará un mensaje de error.

### Explicación de la terminal:

1. **Edición del archivo con `nano`:**
   ```bash
   nano level1.py
   ```
   Aquí abriste el archivo `level1.py` con el editor de texto `nano` para hacer modificaciones. La modificación que mencionas es el condicional `if( user_pw == "8713"):` que compara la contraseña ingresada con `"8713"`.

2. **Ejecución del script `level1.py`:**
   ```bash
   python3 level1.py
   ```
   Al ejecutar el script, te pide que ingreses una contraseña:
   ```bash
   Please enter correct password for flag:
   ```
   Ingresaste `8713`, que es la contraseña correcta:
   ```bash
   8713
   ```

3. **Acción del condicional:**
   Dado que la contraseña que ingresaste (`8713`) coincide con la que se verifica en el condicional `if( user_pw == "8713"):`, el programa continúa ejecutándose dentro de ese bloque y te muestra el mensaje de bienvenida:
   ```bash
   Welcome back... your flag, user:
   ```

4. **Desencriptación de la bandera:**
   Luego, el programa llama a la función `str_xor()`, que se utiliza para descifrar el archivo `level1.flag.txt.enc` usando la contraseña `8713`. Esta función realiza una operación XOR (o "o exclusivo") entre cada carácter del archivo cifrado y la contraseña para descifrar el contenido.

5. **Resultado final:**
   Una vez que la bandera ha sido descifrada, el programa la imprime en pantalla:
   ```bash
   picoCTF{🏁}
   ```

### Descripción detallada de la lógica del código:
- **`str_xor(secret, key)`**: Es una función que realiza la operación XOR entre un mensaje secreto (`secret`) y una clave (`key`). Se asegura de que la clave sea tan larga como el mensaje extendiéndola en un bucle y luego realiza el XOR carácter por carácter.
  
- **Archivo cifrado**: El archivo `level1.flag.txt.enc` contiene la bandera cifrada. El contenido se lee en formato binario (`rb`) y luego se pasa a la función de descifrado `str_xor()`.

- **Comparación de la contraseña**: La línea `if( user_pw == "8713"):` verifica si el usuario ha ingresado la contraseña correcta (`8713`). Si es correcta, el archivo cifrado se descifra y la bandera se muestra al usuario.

### Resumen:
Al agregar el `=` en `if( user_pw == "8713")`, te aseguraste de que el programa verifique correctamente si la contraseña ingresada por el usuario coincide con `"8713"`. Si es correcta, descifra el archivo cifrado y muestra la bandera, en este caso `picoCTF{🏁}`.
