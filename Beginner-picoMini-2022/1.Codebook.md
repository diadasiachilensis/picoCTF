# Codebook

```bash
┌──(kalilinux)-[~/picoCTF]
└─$ ls
┌──(kalilinux)-[~/picoCTF]
└─$ code.py  codebook.txt
┌──(kalilinux)-[~/picoCTF]
└─$ python3 code.py
picoCTF{🏁}
```

### 1. **Lista de archivos en el directorio `~/picoCTF`:**
   ```bash
   ls
   ```
   El comando `ls` lista los archivos en el directorio `~/picoCTF`. La salida indica que tienes al menos dos archivos en ese directorio:
   - `code.py`: Un archivo de Python.
   - `codebook.txt`: Probablemente un archivo de texto que el programa podría usar (aunque no sabemos aún su contenido).

### 2. **Ejecución del archivo Python `code.py`:**
   ```bash
   python3 code.py
   ```
   Este comando ejecuta el archivo `code.py` usando Python 3. Al parecer, el archivo contiene un código que genera la salida `picoCTF{🏁}`. Esta es una bandera común en los desafíos de CTF (Capture The Flag), lo que indica que el script ha sido diseñado para mostrar una solución o un resultado específico para un desafío de seguridad.

### 3. **Salida del script:**
   Al ejecutar el archivo `code.py`, la salida que se muestra es:
   ```
   picoCTF{🏁}
   ```
   Esto es una "bandera" (flag) típica en competiciones CTF, donde debes encontrarla o generar la bandera correcta mediante la resolución de desafíos. El contenido `picoCTF{🏁}` indica que el programa está funcionando correctamente y generando la bandera deseada para el desafío.

### Resumen:
- El archivo `code.py` es un script que, al ejecutarse, genera una bandera típica de un desafío CTF.
- `codebook.txt` es un archivo en el mismo directorio, pero no se ha usado en el comando mostrado. Podría ser un archivo de soporte o de referencia que `code.py` utiliza para generar la salida.
