# runme.py

```bash
┌──(kalilinux㉿hostv6)-[~/picoCTF]
└─$ python3 runme.py
picoCTF{🏁}
```

### 1. **Ejecución del script `runme.py`:**
   ```bash
   python3 runme.py
   ```
   Este comando ejecuta el archivo `runme.py` con Python 3 en tu sistema. En este contexto, el archivo `runme.py` es un script Python que contiene el código necesario para resolver un desafío en la misión de picoCTF.

### 2. **Salida del script `picoCTF{🏁}`:**
   Cuando ejecutas el script, obtienes como resultado la bandera `picoCTF{🏁}`. Esto es un formato estándar en competiciones CTF, donde los participantes deben descubrir o generar "banderas" para completar los desafíos. La bandera normalmente está encerrada entre llaves `{}` y sigue el prefijo `picoCTF`, lo cual es típico de los desafíos de esta competición.